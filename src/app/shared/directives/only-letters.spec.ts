import { OnlyLetters } from './only-letters';

describe('OnlyLetters', () => {
  it('should create an instance', () => {
    const directive = new OnlyLetters();
    expect(directive).toBeTruthy();
  });
});
